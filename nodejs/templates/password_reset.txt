From: "{{sender.name}}" <{{sender.address}}>,
To: "{{user.name}}" <{{user.address}}>
Subject: Password reset request

Someone has requested to reset your password on {{{site.name}}}. To pick a
new password, click the link below.

{{{link}}}

Your username: {{{user.name}}}

If this is a mistake, just ignore this email. The link will expire in 24 hours.

-- 
{{{site.name}}}
{{{site.url}}}
